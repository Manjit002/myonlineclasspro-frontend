# Spring Boot backend (AWS Elastic Beanstalk origin).
# Public base URL only — never put backend secrets or Stripe secret keys in NEXT_PUBLIC_*.
NEXT_PUBLIC_API_BASE_URL=https://main.myonlineclasspro.com
